{
  "#program": "... 30 msec by default without user verify",
  "#user": "{{user.login('<user.email>', '<user.password>')}}",
  "#config": {},
  "#parallel": [
  ],
  "#output" : {
    "filter": "..."
  }
}