{
  "#program": "... 30 msec by default without user verify",
  "#user": "{{user.login(terminal.readline('Username:'), terminal.readline('Password:', 'hidden'))}}",
  "#parallel": [],
  "#output": {
    "filter": "..."
  }
}